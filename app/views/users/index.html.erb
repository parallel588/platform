<h2><%= params[:filter].to_s.titleize %></h2>


<ul>
<% @users.each do |u| %>
  <li>
    <%= u.name_and_email %>
    <% if u.user_type == "seller" %>
      <%= link_to "See the products of #{u.name}.", user_products_path(u) %>
    <% end%>    
  </li>
<% end %>
</ul>


<%= paginate @users %>
